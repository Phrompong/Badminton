interface TableMobileProps {
  className?: string;
}

const TableMobile = ({ className }: TableMobileProps) => {
  return <div className={className}>Table Mobile</div>;
};

export default TableMobile;
